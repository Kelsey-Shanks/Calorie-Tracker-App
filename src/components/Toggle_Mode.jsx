/*
import React from "react";
import Toggle from "react-toggle";
import { useColorScheme } from "./Color_Scheme.jsx";

export const GreyModeToggle = () => {
    const { isGrey, setIsGrey } = useColorScheme();

    return (
        <Toggle
            checked={isGrey}
            onChange={({target }) => setIsGrey(target.checked)}
            aria-label="Grey dark mode toggle"
        />
    );
};
*/